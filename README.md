### ✅ Acceptance Criteria

- ✅ `nx --version` shows the NX version
- ✅ `pnpm install` works, `npm install` is blocked
- ✅ Both apps start without errors
- ✅ You can import MUI components
- ✅ React Router works in both apps