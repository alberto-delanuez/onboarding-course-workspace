apiVersion: v1
kind: Service
metadata:
  name: {{ include "formal-blue.fullname" . }}
  labels:
    {{- include "formal-blue.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "formal-blue.selectorLabels" . | nindent 4 }}
