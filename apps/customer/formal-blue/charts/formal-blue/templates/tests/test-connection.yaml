apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "formal-blue.fullname" . }}-test-connection"
  labels:
    {{- include "formal-blue.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "formal-blue.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
