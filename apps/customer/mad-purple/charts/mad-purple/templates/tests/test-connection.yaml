apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mad-purple.fullname" . }}-test-connection"
  labels:
    {{- include "mad-purple.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mad-purple.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
