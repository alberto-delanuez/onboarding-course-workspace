apiVersion: v1
kind: Service
metadata:
  name: {{ include "mad-purple.fullname" . }}
  labels:
    {{- include "mad-purple.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mad-purple.selectorLabels" . | nindent 4 }}
